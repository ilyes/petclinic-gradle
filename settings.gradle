rootProject.name = 'petstore-gradle'

include 'petstore-domain'
include 'petstore-repo'
include 'petstore-web'

rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
}



